# arjun-b-j.github.io

Source for [arjun-b-j.github.io](https://arjun-b-j.github.io) — my personal portfolio.

Plain HTML / CSS / JS, no build step. Open `index.html` directly or serve the directory locally:

```
python -m http.server 8000
```

GitHub Pages auto-deploys from `master` on every push.
